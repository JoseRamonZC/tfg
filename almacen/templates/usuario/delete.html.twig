<form class="d-inline" method="post" action="{{ path('users_delete', {'id': usuario.id}) }}" onsubmit="return confirm('¿Seguro que quieres borrar este usuario?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usuario.id) }}">
    <button class="boton">Eliminar</button>
</form>